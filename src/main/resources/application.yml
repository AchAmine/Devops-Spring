spring:
  cloud:
    azure:
      keyvault:
        secret:
          property-sources:
            - endpoint: https://myspring-app-keyvault.vault.azure.net/
              credential:
                managed-identity-enabled: true
                client-id: 69421ca9-4587-4d00-b760-34c0249761de
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST}/${DB_NAME}?useSSL=false
    username: ${DB_USERNAME}
    password: ${db-pass}
    hikari:
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
