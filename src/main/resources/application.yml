spring:
  datasource:
    url: jdbc:mysql://myspring-app-sqldb.mysql.database.azure.com:3306/test?serverTimezone=UTC
    username: ${DB_USERNAME}
    password: admin@123
    hikari:
      initialization-fail-timeout: 0
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: create
